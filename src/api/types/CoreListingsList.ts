import type { Listing } from "./Listing";

 export type CoreListingsList200 = Listing[];

 export type CoreListingsListQueryResponse = Listing[];

 export type CoreListingsListQuery = {
    Response: CoreListingsListQueryResponse;
};