import type { Listing } from "./Listing";

export type ListingsList200 = Listing[];

export type ListingsListQueryResponse = Listing[];

export type ListingsListQuery = {
  Response: ListingsListQueryResponse;
};
